# Database Environment Variables (for Drizzle Kit)
# The DATABASE_URL is shared from the api-server.
# Set it here or rely on the root .env

DATABASE_URL=postgresql://user:password@localhost:5432/libraryos
